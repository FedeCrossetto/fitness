/** Defaults numéricos de nutrición — misma fuente para mobile y web. */
export const DEFAULT_KCAL_GOAL = 2200;

export const DEFAULT_MACRO_GOALS = {
  protein: 160,
  carbs: 220,
  fat: 70,
} as const;

export const DEFAULT_HYDRATION_GOAL_ML = 3000;
export const DEFAULT_STEPS_GOAL = 10000;
export const DEFAULT_APP_NAME = 'Reset Fit';
